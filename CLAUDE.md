# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — no source files, no README, no build or dependency manifests, and no prior commit history on any branch. There is no codebase to document yet.

When code is added, this file should be updated to cover:

- **Build / lint / test commands**, including how to run a single test.
- **High-level architecture** that spans multiple files (entry points, module boundaries, data flow, key abstractions) — the kind of context that isn't obvious from reading one file at a time.
- **Project-specific conventions** that differ from defaults (naming, error handling patterns, directory layout rules, commit/PR norms).
- Anything important from a future `README.md`, `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md`.

Re-run `/init` once the initial project scaffolding lands so this file can be populated from real code rather than guesses.
